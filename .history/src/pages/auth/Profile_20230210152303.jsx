import React, { useEffect } from 'react'
import { getAuth } from 'firebase/auth'

export default function Profile() {
  const auth
  useEffect(() => {

  }, [])

  return (
    <div>Profile</div>
  )
}
